
local lx = require('lxlib')

local conf = {
    filter              = '/libTest',
    suffix              = 'Test',
    methodPrefix        = 'test',
    stopOnError         = true,
    stopOnFailure       = true,
    stopOnRisky         = true,
}

return conf

